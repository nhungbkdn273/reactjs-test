import styled from 'styled-components'
import { FlexWrapper } from '../Exercise/styles'

const CalendarContainer = styled(FlexWrapper)`
    max-width: 1250px;
    margin: auto;
    justify-content: space-between;
`

export {CalendarContainer }